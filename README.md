some robots using the becker robots library

policy 71 ?!?!?!?


from the command line:

compile with:

  javac -cp "becker.jar" -d .out *.java

run with:

  java -cp "becker.jar;.out" <program name> <args>

eg:

  java -cp "becker.jar;.out" PaintBot ./PaintWorlds
